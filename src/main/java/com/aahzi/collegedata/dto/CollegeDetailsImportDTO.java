package com.aahzi.collegedata.dto;

import com.fasterxml.jackson.annotation.JsonIgnoreProperties;
import com.fasterxml.jackson.annotation.JsonProperty;
import java.util.List;

@JsonIgnoreProperties(ignoreUnknown = true)
public class CollegeDetailsImportDTO {

    // Helper classes for the JSON structure
    // Since user asked for separate classes previously, I will create separate
    // files for each major object to be safe and clean.
    // This file will just be a placeholder or I can dump them all here if I
    // misinterpret "separate class".
    // The previous request was "move dto to seperate class".
    // I will create separate files for each.
}
